{
  "mcpServers": {
    "mcpbla": {
      "description": "Connect Claude Code to an already running MCPBLA server over HTTP (do not start a local process).",
      "transport": {
        "type": "http",
        "url": "http://127.0.0.1:8000"
      }
    }
  }
}
